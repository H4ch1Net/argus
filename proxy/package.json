{
  "name": "argus-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The backbone service: key/secret broker, CORS shim, HTTPS terminator (Phase 2). OAuth2, AIS websocket, and budget governor arrive in later phases.",
  "scripts": {
    "start": "node server.js",
    "start:https": "node server.js --https",
    "test": "node --test test/"
  },
  "dependencies": {
    "selfsigned": "^2.4.1",
    "ws": "^8.21.3"
  }
}
